info:
  name: GetUser
  type: http
  seq: 11

http:
  method: GET
  url: "{{address}}/{{base_route}}/users/me"
  auth:
    type: bearer
    token: "{{accessToken}}"

runtime:
  variables:
    - name: user_id
      value: "1"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
