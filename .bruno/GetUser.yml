info:
  name: GetUser
  type: http
  seq: 11

http:
  method: GET
  url: "{{address}}/{{base_route}}/users/{{user_id}}"
  auth: inherit

runtime:
  variables:
    - name: user_id
      value: "1"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
