info:
  name: ExportMinecraftServerWorld
  type: http
  seq: 13

http:
  method: GET
  url: "{{address}}/{{base_route}}/minecraft-servers/{{minecraft_server_id}}/world"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
