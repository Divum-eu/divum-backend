info:
  name: Register
  type: http
  seq: 17

http:
  method: POST
  url: "{{address}}/{{base_route}}/auth/register"
  body:
    type: json
    data: |-
      {
        "username": "dimitar",
        "email": "mitko@gmail.com",
        "password": "123"
      }
  auth: inherit

runtime:
  scripts:
    - type: after-response
      code: |-
        let data = res.body;
        bru.setEnvVar("accessToken", data.token)

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
