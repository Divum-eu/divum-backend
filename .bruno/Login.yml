info:
  name: Login
  type: http
  seq: 16

http:
  method: POST
  url: "{{address}}/{{base_route}}/auth/login"
  body:
    type: json
    data: |-
      {
        "username": "dimitar",
        "password": "123"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
