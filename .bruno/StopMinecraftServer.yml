info:
  name: StopMinecraftServer
  type: http
  seq: 3

http:
  method: POST
  url: "{{address}}/{{base_route}}/minecraft-servers/{{minecraft_server_id}}/stop"
  body:
    type: json
    data: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
