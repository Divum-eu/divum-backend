info:
  name: GetMinecraftServer
  type: http
  seq: 10

http:
  method: GET
  url: "{{address}}/{{base_route}}/minecraft-servers/{{minecraft_server_id}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
