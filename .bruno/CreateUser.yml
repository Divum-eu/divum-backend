info:
  name: CreateUser
  type: http
  seq: 5

http:
  method: POST
  url: "{{address}}/{{base_route}}/users"
  body:
    type: json
    data: |-
      {
        "username": "Dimitar",
        "emailAddress": "dimitar@divum.eu",
        "password": "Password123!"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
