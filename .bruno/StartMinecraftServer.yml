info:
  name: StartMinecraftServer
  type: http
  seq: 2

http:
  method: POST
  url: "{{address}}/{{base_route}}/mc-server-instances/8d110d61-4a6d-4090-b03b-edca190cebc3/start"
  body:
    type: json
    data: ""
  auth: inherit

runtime:
  variables:
    - name: server_id
      value: "4"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
