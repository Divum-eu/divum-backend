info:
  name: EditUser
  type: http
  seq: 8

http:
  method: PATCH
  url: "{{address}}/{{base_route}}/users/{{user_id}}"
  body:
    type: json
    data: |-
      {
        "username": "Dimitar2"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
